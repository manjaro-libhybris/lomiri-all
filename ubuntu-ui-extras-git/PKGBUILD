# Maintainer: Marius Gripsgard <marius@ubports.com>

pkgname=ubuntu-ui-extras-git
_pkgname=ubuntu-ui-extras
pkgver=r602.8fecad1
pkgrel=1
pkgdesc="Lomiri UI toolkit extras for Mobile Devices"
arch=('x86_64' 'armv7' 'aarch64')
url="https://github.com/ubports/ubuntu-ui-extras.git"
license=('GPL3')
depends=(qt5-base qt5-declarative libcups exiv2)
makedepends=('git' 'cmake' 'cmake-extras')
source=('git+https://github.com/ubports/ubuntu-ui-extras.git')
md5sums=('SKIP')

pkgver() {
  cd ${_pkgname}
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

prepare() {
  mkdir -p build
  cd ${_pkgname}
}

build() {
  cd build
  cmake ../${_pkgname} . -DCMAKE_INSTALL_PREFIX:PATH=/usr -DCMAKE_INSTALL_LIBDIR="lib/" -DCMAKE_INSTALL_SYSCONFDIR=/etc -DCMAKE_INSTALL_LOCALSTATEDIR=/var -DCLICK_MODE=off
  make
}

package() {
  cd build
  make DESTDIR="$pkgdir/" install
  mv "${pkgdir}/usr/lib/qt5/" "${pkgdir}/usr/lib/qt/"
}
